@model NSMkt.Models.VM.CardModel

<div class="card border-left-@Model.css bg-gradient-@Model.css shadow h-100 py-2">
			<div class="card-body">
				<div class="row no-gutters align-items-center">
					<div class="col mr-2">
						<div class="text-xs font-weight-bold text-white  text-uppercase mb-1">
							@Model.display
						</div>
						<div class="h5 mb-0 font-weight-bold text-white">
						<a class="text-light" href=@Model.url>@Model.display </a></div>
					</div>
					@*<div class="col-auto">
						<i class="fas fa-calendar fa-2x text-gray-300"></i>
					</div>*@
				</div>
			</div>
		</div>
