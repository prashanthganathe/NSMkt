				<!-- Topbar -->
<nav class="navbar navbar-expand navbar-light bg-white topbar mb-4 static-top shadow">

	<!-- Sidebar Toggle (Topbar) -->
	<button id="sidebarToggleTop" class="btn btn-link d-md-none rounded-circle mr-3">
		<i class="fa fa-bars"></i>
	</button>


	<!-- Topbar Search -->
	<form class="d-none d-sm-inline-block form-inline mr-auto ml-md-3 my-2 my-md-0 mw-100 navbar-search">

		<div class="input-group">
			@*  <input type="text" class="form-control bg-light border-0 small" placeholder="Search for..."  aria-label="Search" aria-describedby="basic-addon2">*@

			@*	@{
				var script = Context.Session.Get(SessionKeys.script).ToString();
				if (script == null)
				script = "BANKNIFTY";
				}

				<select>
				@{var country_list = (List<CP.Models.Country_List>)Session["CountryList"];
				foreach (var item in country_list)
				{
				if (item.Name == script)
				{
				<option value="@item.ID" selected>@item</option>
				}
				else
				{
				<option value="@item.ID">BANKNIFTY</option>
				}

				}
				}
				</select>*@


		    <partial name="_dropdownScript" />
		    <partial name="_dropdownExpiry" />

	
			<div class="input-group-append">
				<button class="btn form-user btn-success" type="button">Submit</button>
				<a href="#" class="btn btn-success btn-icon-split">
					<span class="icon text-white-50">
						<i class="fas fa-arrow-right"></i>
					</span>
				</a>
			</div>
		</div>
	</form>

	<!-- Topbar Navbar -->
	<div class="col-xl-2">
		<ul class="navbar-nav ml-auto">		
		<partial name="_notification" />
			<partial name="_messages" />
			<div class="topbar-divider d-none d-sm-block"></div>
			<!-- Nav Item - User Information -->
			<li class="nav-item dropdown no-arrow">
				<partial name="_LoginPartial" />
			</li>
		</ul>
	</div>
</nav>
<!-- End of Topbar -->				<!-- Topbar -->
